name: 'apex_ml'
version: '1.0.0'
config-version: 2

profile: 'apex_ml'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  apex_ml:
    staging:
      +materialized: view
      +schema: staging
    analytics:
      +materialized: table
      +schema: prod
