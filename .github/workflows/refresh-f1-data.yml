name: Refresh F1 Race Data

on:
  schedule:
    # Run every hour on race weekends (Fri-Sun) for live ML training
    - cron: '0 */6 * * 5,6,0'
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: read

jobs:
  refresh-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          cd snowflake/elt
          uv sync

      - name: Set up Snowflake private key
        run: |
          mkdir -p ~/.ssh
          cat > ~/.ssh/snowflake_key.p8 << 'EOF'
          ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          EOF
          chmod 600 ~/.ssh/snowflake_key.p8

      - name: Run data refresh pipeline
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_DATABASE: APEXML_DEV
          SNOWFLAKE_SCHEMA: DEV
          SNOWFLAKE_WAREHOUSE: ETL_WH_DEV
          SNOWFLAKE_ROLE: DATA_ENGINEER
        run: |
          cd snowflake/elt
          uv run python refresh_latest.py

      - name: Notify on failure
        if: failure()
        run: echo "Data refresh failed! Check logs."
